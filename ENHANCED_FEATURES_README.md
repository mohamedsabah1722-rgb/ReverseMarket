# 📋 تحسينات نظام إدارة الطلبات والإشعارات والمتاجر

## 🎯 نظرة عامة
تم تطوير وتحسين نظام شامل لإدارة الطلبات والإشعارات والمتاجر مع تجربة مستخدم محسنة للهاتف المحمول ودعم متعدد اللغات.

---

## ✨ الميزات الجديدة المضافة

### 1️⃣ نظام الصورة الافتراضية للطلبات
- **الملفات المضافة:**
  - `Services/IDefaultImageService.cs`
  - `Services/DefaultImageService.cs`
  - `wwwroot/images/default-request.svg`

- **الوظائف:**
  - إضافة صورة افتراضية تلقائياً عند عدم رفع صورة للطلب
  - صورة SVG محسنة وخفيفة الحجم
  - تكامل مع نظام رفع الصور الموجود

### 2️⃣ تفضيلات الإشعارات للمستخدمين
- **التحديثات:**
  - إضافة خصائص جديدة لـ `ApplicationUser`:
    - `EnableWhatsAppNotifications`
    - `EnableEmailNotifications`
    - `EnableInAppNotifications`
  - تحديث `NotificationService` لاحترام تفضيلات المستخدم
  - إنشاء migration لقاعدة البيانات

### 3️⃣ خدمة سير عمل الطلبات المحسنة
- **الملفات المضافة:**
  - `Services/IRequestWorkflowService.cs`
  - `Services/RequestWorkflowService.cs`

- **الوظائف:**
  - إدارة مركزية لجميع إشعارات الطلبات
  - إشعارات ذكية للمتاجر المتخصصة
  - تتبع دقيق لحالات الطلبات
  - دعم إشعارات التعديل والحذف

### 4️⃣ خدمة سير عمل المتاجر
- **الملفات المضافة:**
  - `Services/IStoreWorkflowService.cs`
  - `Services/StoreWorkflowService.cs`

- **الوظائف:**
  - إشعارات إنشاء المتاجر للإدارة
  - إشعارات الموافقة/الرفض لأصحاب المتاجر
  - إدارة تعديلات المتاجر
  - إشعارات الحذف

### 5️⃣ واجهة الفئات المحسنة للهاتف المحمول
- **التحسينات:**
  - نظام checkbox محسن بدلاً من select متعدد
  - واجهة تفاعلية قابلة للمس
  - عرض تدريجي للفئات (خطوة بخطوة)
  - تصميم متجاوب لجميع أحجام الشاشات

### 6️⃣ دعم شامل للغة الكردية
- **الملفات المضافة:**
  - `wwwroot/css/kurdish-support.css`
  - تحديث `Views/Shared/_LanguageSelector.cshtml`

- **الميزات:**
  - خطوط محسنة للنص الكردي
  - دعم RTL للواجهة
  - تحسينات للهاتف المحمول
  - إصلاح مشكلة عرض الأعلام

---

## 🔄 سير العمل المحسن

### إدارة الطلبات:
1. **إضافة طلب جديد** → إشعار للمدير فقط
2. **موافقة على الطلب** → إشعار للمستخدم + المتاجر المتخصصة
3. **رفض الطلب** → إشعار للمستخدم مع السبب
4. **تعديل الطلب** → إشعار للمدير → قرار → إشعار للمستخدم
5. **حذف الطلب** → إشعار للمدير والمستخدم

### إدارة المتاجر:
1. **إنشاء متجر** → إشعار للمدير
2. **موافقة/رفض** → إشعار لصاحب المتجر
3. **تعديل المتجر** → إشعار للمدير → قرار → إشعار لصاحب المتجر
4. **حذف المتجر** → إشعار للمدير وصاحب المتجر

---

## 📱 تحسينات الهاتف المحمول

### واجهة الفئات:
- نظام checkbox تفاعلي
- عرض تدريجي للفئات
- أزرار كبيرة قابلة للمس
- تصميم متجاوب

### اختيار اللغة:
- أعلام محسنة العرض
- قائمة منسدلة محسنة
- دعم أفضل للنص الكردي
- تموضع صحيح على الشاشات الصغيرة

---

## 🛠️ الملفات المحدثة

### Controllers:
- `Controllers/RequestsController.cs`
- `Areas/Admin/Controllers/RequestsController.cs`

### Services:
- `Services/NotificationService.cs` (محدث)
- `Services/IDefaultImageService.cs` (جديد)
- `Services/DefaultImageService.cs` (جديد)
- `Services/IRequestWorkflowService.cs` (جديد)
- `Services/RequestWorkflowService.cs` (جديد)
- `Services/IStoreWorkflowService.cs` (جديد)
- `Services/StoreWorkflowService.cs` (جديد)

### Models:
- `Models/Identity/ApplicationUser.cs` (محدث)

### Views:
- `Views/Requests/Create.cshtml` (محدث)
- `Views/Shared/_LanguageSelector.cshtml` (محدث)
- `Views/Shared/_Layout.cshtml` (محدث)

### CSS:
- `wwwroot/css/kurdish-support.css` (جديد)

### Database:
- `Migrations/20251218234500_AddNotificationPreferences.cs` (جديد)

### Configuration:
- `Program.cs` (محدث لتسجيل الخدمات الجديدة)

---

## 🚀 كيفية النشر

### 1. تحديث قاعدة البيانات:
```bash
dotnet ef database update
```

### 2. التأكد من الملفات:
- تأكد من وجود `wwwroot/images/default-request.svg`
- تأكد من وجود `wwwroot/css/kurdish-support.css`

### 3. اختبار الميزات:
- اختبار إنشاء طلب بدون صورة
- اختبار الإشعارات للمتاجر المتخصصة
- اختبار واجهة الفئات على الهاتف
- اختبار تبديل اللغة الكردية

---

## 📋 قائمة التحقق للاختبار

### ✅ الطلبات:
- [ ] إنشاء طلب بدون صورة (يجب إضافة صورة افتراضية)
- [ ] موافقة المدير على طلب (إشعار للمستخدم والمتاجر)
- [ ] رفض طلب مع سبب (إشعار للمستخدم فقط)
- [ ] تعديل طلب (إشعار للمدير ثم للمستخدم)
- [ ] حذف طلب (إشعار للمدير والمستخدم)

### ✅ المتاجر:
- [ ] إنشاء متجر جديد (إشعار للمدير)
- [ ] موافقة على متجر (إشعار لصاحب المتجر)
- [ ] رفض متجر (إشعار مع السبب)
- [ ] تعديل متجر (سير عمل كامل)

### ✅ الواجهة:
- [ ] اختيار الفئات على الهاتف المحمول
- [ ] تبديل اللغة (خاصة الكردية)
- [ ] عرض الأعلام بشكل صحيح
- [ ] النص الكردي واضح ومقروء

### ✅ الإشعارات:
- [ ] تفضيلات الإشعارات تعمل
- [ ] الإشعارات تصل للمستخدمين المناسبين
- [ ] المتاجر تتلقى إشعارات الطلبات المتخصصة فقط

---

## 🔧 استكشاف الأخطاء

### مشاكل محتملة:
1. **الصورة الافتراضية لا تظهر:** تأكد من وجود الملف في `wwwroot/images/`
2. **النص الكردي غير واضح:** تأكد من تحميل `kurdish-support.css`
3. **الإشعارات لا تصل:** تحقق من تسجيل الخدمات في `Program.cs`
4. **واجهة الفئات لا تعمل:** تحقق من JavaScript في `Create.cshtml`

### حلول سريعة:
- إعادة تشغيل التطبيق بعد تحديث `Program.cs`
- مسح cache المتصفح لرؤية تحديثات CSS
- التأكد من صحة migration قاعدة البيانات

---

## 📞 الدعم الفني

للمساعدة في أي مشاكل تقنية أو أسئلة حول الميزات الجديدة، يرجى:
1. مراجعة هذا الدليل أولاً
2. التحقق من logs التطبيق
3. اختبار الميزات خطوة بخطوة حسب قائمة التحقق

---

**تاريخ آخر تحديث:** 18 ديسمبر 2024  
**الإصدار:** 2.0 - Enhanced Orders & Notifications System